#include "utils.h"
/*
float roundf(float value, uint8_t precision) {
    float multiplier = 1.0f;
    for (uint8_t i = 0; i < precision; i++) {
        multiplier *= 10.0f;
    }
    return (float)((int)(value * multiplier + 0.5f)) / multiplier;
}

uint32_t roundu(uint32_t value, uint8_t precision) {
    uint32_t multiplier = 1;
    for (uint8_t i = 0; i < precision; i++) {
        multiplier *= 10;
    }
    return (value + multiplier / 2) / multiplier * multiplier;
}
*/